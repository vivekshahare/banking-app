spring:
  application:
    name: banking-app

  datasource:
    url: jdbc:postgresql://localhost:5432/banking_app
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true